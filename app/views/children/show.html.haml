%nav
  %p.hint Le patient se présente à nouveau au dispensaire ?
  - if Csps.point?
    = link_to 'Nouvelle consulation', new_child_diagnostic_path(@child), :class => 'new-diagnostic'
  
= partial 'children/profile'

%h2.em Consultations

%section.list-arrow
  %ul
    - for diag in @child.diagnostics.order('id DESC').limit(5) do
      %li{ :class => cycle("odd", "even") }= partial 'diagnostics/header', locals: { diagnostic: diag }
