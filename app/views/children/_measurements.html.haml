%table
  %tbody
    %tr
      %th(scope='row') Taille
      %td #{diagnostic.height} cm
      %th(scope='row') Poids
      %td #{diagnostic.weight} kg
      %th(scope='row') Perim. Brac.
      %td #{diagnostic.mac} cm
