%span.name= child.name
%p
  %strong #{child.gender ? 'Né' : 'Née'} le :
  %span.age{'data-years' => age_in_years(child.born_on)}
    #{l child.born_on}, #{textual_age child.born_on}
  %br/
  %strong Village : 
  #{child.village.try(:name) || '-'}
%button.edit(type="button") Editer
