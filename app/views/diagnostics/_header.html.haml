- link_to child_diagnostic_path(@child, diagnostic) do
  %strong
    = l(diagnostic.done_on, :format => :diagnostic)
    (#{t(:from, :scope => :datetime)}
    #{time_ago_in_words(diagnostic.done_on)})
  %span.by Consultation réalisée par #{diagnostic.author.login}
